import React from 'react';

const Footer = () => (
  <footer className="py-8 border-t border-cream-200 text-center text-xs font-semibold text-toast-400">
    <p>Designed and built with performance in mind. &copy; {new Date().getFullYear()}</p>
  </footer>
);

export default Footer;
